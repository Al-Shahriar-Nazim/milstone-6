// variableName . reduce( (previos, currentvalue) => previos+currentvalue, initialValues)

// --------------------reduce()-------------------------------------------------------------------
//..........................................................................................................................
const number = [2, 2,2];
const result = number.reduce(
  (previosValue, currentValue) => previosValue + currentValue,
  0
);
console.log(result);
//..........................................................................................................................

